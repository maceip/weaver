package com.weaver.app.ui.theme

import androidx.compose.ui.graphics.Color

// Voltage accent (default) — lightning-white blue on deep navy ground.
val Voltage = Color(0xFFC5E8FB)
val VoltageDim = Color(0x38C5E8FB)
val VoltageInk = Color(0xFF04101C)

// Alternate accents (swap into the color scheme to retheme).
val Hyperblue = Color(0xFF4DA8FF)
val Midnight = Color(0xFF6B7BFF)
val Aero = Color(0xFF7CC9F3)

// Canvas surfaces — deep, near-black with a hint of blue.
val Bg = Color(0xFF0A0B0E)
val Surface1 = Color(0xFF15161B)
val Surface2 = Color(0xFF1C1D22)

// Wireframe blocks inside artboards.
val Block = Color(0xFF2A2C33)
val BlockHi = Color(0xFF33353D)

// Lines + text.
val Line = Color(0x12FFFFFF)
val LineStrong = Color(0x24FFFFFF)
val TextPrimary = Color(0xFFF3F5F8)
val TextDim = Color(0x8CF3F5F8)
val TextFaint = Color(0x59F3F5F8)

// Retained for legacy callers — point them at the Voltage palette so we don't fork.
val Purple80 = Color(0xFFD0BCFF)
val PurpleGrey80 = Color(0xFFCCC2DC)
val Pink80 = Color(0xFFEFB8C8)
val Purple40 = Color(0xFF6650A4)
val PurpleGrey40 = Color(0xFF625B71)
val Pink40 = Color(0xFF7D5260)
