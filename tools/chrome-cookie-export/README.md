# Chrome cookie export (Weaver)

Load unpacked in Chrome/Canary: **Extensions → Manage extensions → Developer mode → Load unpacked** → select this folder.

Click the extension icon → **Export JSON** or **Copy to clipboard**. Use the file with `scripts/push-cookies-to-device.sh` when the Pixel is on USB.
